{
  "name": "ubccs",
  "version": "1.0.0",
  "description": "UBC CS Faculty Theme",
  "license": "MIT",
  "scripts": {
    "sass": "node-sass --recursive --source-map true --output-style expanded --output css sass",
    "autoprefixer": "postcss --no-map -u autoprefixer --autoprefixer.browsers -r css/*.css",
    "build:css": "npm run sass && npm run autoprefixer",
    "build:images": "imagemin img/* -o img",
    "watch:css": "onchange 'sass/**/*.scss' -- npm run build:css",
    "watch:img": "onchange 'img/**/*' -- npm run build:images",
    "watch:all": "concurrently --kill-others 'npm run watch:css' 'npm run watch:img'"
  },
  "dependencies": {
    "autoprefixer": "^9.6.1",
    "bootstrap": "^4.3.1",
    "concurrently": "^4.1.1",
    "imagemin-cli": "^4.0.1",
    "node-sass": "^4.12.0",
    "normalize.css": "github:necolas/normalize.css",
    "onchange": "^6.0.0",
    "postcss-cli": "^6.1.3"
  },
  "browserslist": [
    "> 1%"
  ]
}
